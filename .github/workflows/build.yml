jobs:
  Build-Kernel:
    name: "🐎 Build OnePlus Kernel with repo manifest"
    runs-on: ubuntu-latest
    steps:
      - name: "✨ Install repo tool"
        run: |
          sudo apt-get update
          sudo apt-get install -y python3
          mkdir -p ~/bin
          curl -o ~/bin/repo https://storage.googleapis.com/git-repo-downloads/repo
          chmod a+x ~/bin/repo
          echo 'export PATH=~/bin:$PATH' >> ~/.bashrc
          source ~/.bashrc

      - name: "🌟 Initialize source using manifest"
        run: |
          repo init -u https://github.com/OnePlusOSS/kernel_manifest.git -b oneplus/sm8450 -m oneplus_10_pro_s.xml --depth=1
          repo sync
          echo "Source initialized and synced."

      - name: "✨ Build Kernel"
        run: |
          # Continue with kernel build commands here
